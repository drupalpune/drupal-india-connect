A single big number with a caption. Used in rows of three or four, usually on a dark band.

```jsx
<StatCard stat="60+" description="Sessions across four tracks" />
```

Only use real, verifiable numbers — an invented stat is worse than no stat. Inside a `Section` with `darkBackground`, the figure goes white and the caption goes to the secondary colour automatically.
