Inline video for body content.

```jsx
<Video src="assets/teaser.mp4" type="video/mp4" poster="assets/teaser.webp" ratio="16:9" />
```

The homepage hero video is NOT this component — that lives inside `SiteHero25`, which owns its own play/pause control and gradient overlay.
