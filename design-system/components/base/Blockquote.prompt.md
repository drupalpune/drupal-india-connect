Pull quote for body copy — italic, responsive, with a faint oversized quote mark bleeding off the left edge.

```jsx
<Blockquote quote="The hallway track alone paid for the trip." name="Priya R." cite="Platform lead, Singapore" />
```

For testimonial grids use `CardTestimonial` instead; this one is for inline editorial use.
